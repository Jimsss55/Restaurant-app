<div class="min-h-full flex justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div>
      <h2 class="mt-16 text-center text-3xl font-extrabold text-gray-900">
        Forgot your password?
      </h2>
    </div>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <div class="mt-8 space-y-6">
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="rounded-md shadow-sm -space-y-px">
          <div>
            <%= f.label :email, class: "sr-only", for: "email-address" %>
            <%= f.email_field :email, id: "email-address", autofocus: true, autocomplete: "email",
                              placeholder: "Email address", class: "appearance-none rounded-none relative block
                              w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900
                              rounded-t-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500
                              focus:z-10 sm:text-sm" %>
          </div>
        </div>

        <div class="actions">
          <%= f.submit "Send Reset Password instruction", class: "actions group relative w-full flex justify-center
                                  py-2 px-4 border-transparent
                                  text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700
                                  focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"%>
        </div>

        <div class="flex items-center justify-center">
          <div class="text-sm">
            <%= link_to "Log in", new_user_session_path, class: "font-bold text-indigo-600 hover:text-indigo-500" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>